interface:
  display_name: "Su React Prototype System"
  short_description: "Build Figma-aligned React systems with Tailwind"
  default_prompt: "Use $react-prototype-system to preserve or infer Figma names, map its design system into Tailwind variables, build responsive TypeScript components in matching files, and compose the page from them."
